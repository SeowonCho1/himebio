PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/bio_trade
JWT_SECRET=change-me-to-long-random-string
CLIENT_URL=http://localhost:5173

# SMTP (optional — inquiries still save if mail fails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_TO=admin@example.com

# Cloudinary (optional — without it, use image URLs in admin)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
