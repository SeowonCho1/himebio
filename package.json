{
  "name": "hime-biomed-monorepo",
  "private": true,
  "description": "Express API + Next.js client (프론트는 Next standalone 또는 dev 서버로 실행)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --prefix client && npm run build --prefix client && npm ci --prefix server",
    "start": "npm start --prefix server"
  }
}
